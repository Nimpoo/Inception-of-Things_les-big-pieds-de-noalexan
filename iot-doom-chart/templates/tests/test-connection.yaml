apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "iot-doom-chart.fullname" . }}-test-connection"
  labels:
    {{- include "iot-doom-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "iot-doom-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
